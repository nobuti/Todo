define ['buti'], (Buti)->
  describe "Boolean", ->
    it "should true when the value is true", ->
        expect(true).to.equal true

  describe "Array", ->
    describe "#indexOf()", ->
      it "should return -1 when the value is not present", ->
        expect(true).to.equal true
        expect([ 1, 2, 3 ].indexOf(5)).to.equal -1
        expect([ 1, 2, 3 ].indexOf(5)).to.equal -1

  describe 'Example', ->
    describe '#truth()', ->
      it 'should return Boolean true', ->
        console.log Buti #property for property of Module
        expect(new Buti()).to.not.be.undefined
        expect(new Buti().method()).to.equal true

          
return      